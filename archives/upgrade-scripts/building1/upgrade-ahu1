#!/usr/bin/env bash

python ./scripts/install-agent.py \
    -s transactivecontrol/MarketAgents/AHUAgent \
    -i platform.ahu1 \
    --config config/config/building1/AHU1.config \
    --tag ahu \
    --start \
    --force
