#!/usr/bin/env bash

python ./scripts/install-agent.py \
    -s transactivecontrol/MarketAgents/AHUAgent \
    -i platform.ahu2 \
    --config config/config/building1/AHU2.config \
    --tag ahu \
    --start \
    --force
