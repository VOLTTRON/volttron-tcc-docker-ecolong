
#!/usr/bin/env bash

# Build a temp file string for use with the configuration

python scripts/install-agent.py \
    -s volttron/TNS_building_Agent \
    --config config/config/building1/BUILDING1.config  \
    --tag tns_building \
    -i tns.building_agent \
    --force \
    --start
